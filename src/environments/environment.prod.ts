// src/environments/environment.prod.ts
export const environment = {
    production: true,

    /**
     * Base API URL for production deployment.
     * Replace with your real backend URL when deploying.
     */
    apiUrl: 'https://your-production-api.com'
};
