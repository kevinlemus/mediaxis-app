// src/environments/environment.ts
export const environment = {
    production: false,

    /**
     * Base API URL for local development.
     * All Angular services should build their endpoints from this.
     */
    apiUrl: 'http://localhost:8080'
};
